import { Component } from '@angular/core';

@Component({
  selector: 'app-root',
  template: `Test case management is ready to start`,
})
export class AppComponent {
  title = 'testcasemanagement';
}
